import { StringConverterPipe } from './string-converter.pipe';

describe('StringConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new StringConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
